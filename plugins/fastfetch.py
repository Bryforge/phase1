# phase1 reserves this plugin name for the built-in Rust fastfetch renderer.
# The shell intercepts `fastfetch` before plugin dispatch so it can render
# privacy-safe live system facts without host-tool access.
print('phase1 fastfetch is built into the Rust shell')
