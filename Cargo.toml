[package]
name = "phase1"
version = "3.3.2"
edition = "2021"
authors = ["Chase Bryan"]
description = "Terminal-based educational OS simulator in Rust."
license = "GPL-3.0"
readme = "README.md"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
